import { LoadingComponent } from "@/components/loading-components";

export default function Loading() {
  return (
    <main className="w-full h-screen bg-background grid place-items-center">
      <LoadingComponent />
    </main>
  )
}